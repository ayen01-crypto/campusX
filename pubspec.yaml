name: campusx
description: CampusX - everything campus, one app.
publish_to: none
version: 0.2.0+2

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.1
  go_router: ^17.3.0
  shared_preferences: ^2.5.5
  dio: ^5.11.0
  flutter_secure_storage: ^10.3.1
  connectivity_plus: ^7.3.1
  socket_io_client: ^3.1.6
  image_picker: ^1.2.3
  file_picker: ^11.0.2
  url_launcher: ^6.3.2
  uuid: ^4.6.0
  intl: ^0.20.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
